import { AlturaPesoPipe } from './altura-peso.pipe';

describe('AlturaPesoPipe', () => {
  it('create an instance', () => {
    const pipe = new AlturaPesoPipe();
    expect(pipe).toBeTruthy();
  });
});
